apiVersion: v1
kind: Service
metadata:
  name: ml-inference-service
  labels:
    app: ml-inference
spec:
  type: NodePort
  selector:
    app: ml-inference
  ports:
  - name: http
    port: 8001
    targetPort: 8001
    nodePort: 30001